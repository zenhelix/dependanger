= F005: BOM Generation
:toc: left
:toclevels: 3
:sectnums:
:icons: font

[abstract]
--
Генерация Maven BOM (Bill of Materials) из EffectiveMetadata.
Альтернативный выходной формат для Maven-экосистемы.
Артефакт публикуется для использования в других Maven/Gradle-проектах организации.
--

== Метаданные

[cols="1,2"]
|===
| ID | F005
| Название | BOM Generation
| Приоритет | P2 (важно)
| Статус | 🔴 Не начат
| Модуль | `dependanger-generator-bom`
| Зависит от | F003 (Processing Pipeline)
| Блокирует | —
|===

== Описание

BOM Generation преобразует `EffectiveMetadata` в Maven BOM — POM-файл для централизованного управления версиями в Maven и Gradle (через platform).

== User Stories

=== US-001: Генерация Maven BOM

[quote]
____
Как Maven разработчик, я хочу получить BOM из моего каталога зависимостей, чтобы использовать централизованное управление версиями в Maven проектах.
____

**Acceptance Criteria:**
* Генерируется валидный POM файл * Содержит dependencyManagement секцию * Совместим с Maven 3.x

=== US-002: Использование BOM в Gradle

[quote]
____
Как Gradle разработчик, я хочу использовать сгенерированный BOM как platform, чтобы не дублировать версии.
____

**Acceptance Criteria:**
* BOM можно использовать через `platform()`
* Версии из BOM применяются к зависимостям

== SPI: ArtifactGenerator

=== BomGenerator

`BomGenerator` реализует генерацию Maven BOM (Bill of Materials). Принимает `EffectiveMetadata` и создаёт POM файл с секцией `<dependencyManagement>` содержащей все библиотеки с версиями.

**Параметры:**

* `groupId` — Maven groupId для генерируемого BOM
* `artifactId` — Maven artifactId для генерируемого BOM
* `version` — версия BOM

== Формат BOM

=== Структура

[source,xml]
----
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <groupId>com.example</groupId>
    <artifactId>dependanger-bom</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    
    <name>Dependanger BOM</name>
    <description>Generated by Dependanger</description>
    
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-stdlib</artifactId>
                <version>2.1.0</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter</artifactId>
                <version>3.2.0</version>
            </dependency>
            <!-- ... остальные зависимости ... -->
        </dependencies>
    </dependencyManagement>
</project>
----

== Gradle Task

=== dependangerGenerateBom

Задача `dependangerGenerateBom` генерирует Maven BOM из EffectiveMetadata.

**Inputs:**

* `effective-{distribution}.json` от предыдущего таска

**Outputs:**

* `build/dependanger/bom.pom.xml`

**Опции:**

* `bomGroupId`, `bomArtifactId`, `bomVersion` — координаты генерируемого BOM
* `includeName`, `includeDescription` — добавлять name/description в POM

== Использование

=== В Maven

[source,xml]
----
<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>com.example</groupId>
            <artifactId>dependanger-bom</artifactId>
            <version>1.0.0</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>

<dependencies>
    <dependency>
        <groupId>org.jetbrains.kotlin</groupId>
        <artifactId>kotlin-stdlib</artifactId>
        <!-- версия из BOM -->
    </dependency>
</dependencies>
----

=== В Gradle

[source,kotlin]
----
dependencies {
    // Импорт BOM как platform
    implementation(platform("com.example:dependanger-bom:1.0.0"))
    
    // Версии автоматически из BOM
    implementation("org.jetbrains.kotlin:kotlin-stdlib")
    implementation("org.springframework.boot:spring-boot-starter")
}
----

== Публикация BOM

=== Gradle Publishing

[source,kotlin]
----
publishing {
    publications {
        create<MavenPublication>("bom") {
            // Используем сгенерированный POM
            pom.withXml {
                val bomFile = tasks.named("dependangerGenerateBom").get().outputFile.get().asFile
                asNode().replaceNode(XmlParser().parse(bomFile))
            }
            
            groupId = "com.example"
            artifactId = "dependanger-bom"
            version = project.version.toString()
        }
    }
}
----

== Валидация

[cols="1,2,1",options="header"]
|===
| Правило | Описание | Severity

| Уникальность координат
| Нет дубликатов group:artifact
| ERROR

| Валидные координаты
| groupId и artifactId соответствуют Maven spec
| ERROR

| Валидные версии
| Версии не пустые
| ERROR

| XML синтаксис
| Валидный XML
| ERROR
|===

== Отличия от TOML

[cols="1,2,2",options="header"]
|===
| Аспект | TOML (Version Catalog) | BOM

| Формат
| TOML
| XML (POM)

| Экосистема
| Gradle 7.0+
| Maven, Gradle (через platform)

| Бандлы
| Поддерживаются
| Не поддерживаются

| Плагины
| Поддерживаются
| Не поддерживаются

| Публикация
| Копирование файла
| Maven artifact
|===

== Связи

* **Входные данные:** `EffectiveMetadata` от xref:F003-processing-pipeline.adoc[F003]
* **Выходные данные:** `bom.pom.xml`

== Связанные документы

* xref:../01-concepts/architecture.adoc[Архитектура — ArtifactGenerator SPI]
* https://maven.apache.org/guides/introduction/introduction-to-dependency-mechanism.html#bill-of-materials-bom-poms[Maven BOM]
