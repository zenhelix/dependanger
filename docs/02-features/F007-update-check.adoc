= F007: Update Check
:toc: left
:toclevels: 3
:sectnums:
:icons: font

[abstract]
--
Проверка доступных обновлений для библиотек в Maven репозиториях.
Опциональный процессор в pipeline (order: 70).
--

== Метаданные

[cols="1,2"]
|===
| ID | F007
| Название | Update Check
| Приоритет | P3 (желательно)
| Модуль | `dependanger-updates`
| Зависит от | F003 (Processing Pipeline)
| Блокирует | —
|===

== Описание

Update Check сканирует Maven репозитории для определения доступных обновлений библиотек.
Результат добавляется в `Diagnostics` и может быть включён в отчёт.

== User Stories

=== US-001: Проверка обновлений

[quote]
____
Как разработчик, я хочу видеть какие библиотеки имеют доступные обновления, чтобы своевременно обновлять зависимости.
____

**Acceptance Criteria:**

[cols="1,3,3,3",options="header"]
|===
| ID | Given | When | Then

| AC-001-1
| Библиотека `kotlin-stdlib:2.1.0`, в Maven Central есть `2.1.20`
| Запускаю pipeline с `withUpdateCheck()`
| В diagnostics появляется `UpdateAvailableInfo` с `updateType = PATCH`

| AC-001-2
| Библиотека `spring-boot:3.2.0`, в Maven Central есть `3.4.0`
| Запускаю проверку обновлений
| `UpdateAvailableInfo` содержит `currentVersion=3.2.0`, `latestStable=3.4.0`, `updateType=MINOR`

| AC-001-3
| Библиотека `ktor:2.3.0`, есть `3.0.0`
| Запускаю проверку обновлений
| `updateType = MAJOR`

| AC-001-4
| Библиотека `lib:1.0.0`, есть только `2.0.0-RC1`
| Запускаю с `includePrerelease = true`
| `latestAny = 2.0.0-RC1`, `updateType = PRERELEASE`

| AC-001-5
| Библиотека `lib:1.0.0`, есть только `2.0.0-RC1`
| Запускаю с `includePrerelease = false` (default)
| Обновление не показывается (нет стабильной версии новее)

| AC-001-6
| 10 библиотек в каталоге, 3 имеют обновления
| Запускаю проверку обновлений
| В diagnostics ровно 3 `UpdateAvailableInfo`
|===

=== US-002: Интеграция в CI

[quote]
____
Как DevOps инженер, я хочу запускать проверку обновлений в CI, чтобы автоматически уведомлять команду о доступных обновлениях.
____

**Acceptance Criteria:**

[cols="1,3,3,3",options="header"]
|===
| ID | Given | When | Then

| AC-002-1
| Dependanger CLI установлен
| Запускаю `dependanger check-updates`
| Выводится список библиотек с доступными обновлениями

| AC-002-2
| Dependanger CLI установлен
| Запускаю `dependanger check-updates --json`
| Вывод в JSON формате, пригодном для парсинга

| AC-002-3
| Gradle проект с Dependanger plugin
| Запускаю `./gradlew dependangerCheckUpdates`
| Task выполняется, результат в консоли и в `build/dependanger/updates.json`

| AC-002-4
| CI pipeline с проверкой обновлений
| Есть MAJOR обновления
| Exit code = 0 (по умолчанию), но можно настроить `failOnMajorUpdates = true`
|===

=== US-003: Фильтрация обновлений

[quote]
____
Как разработчик, я хочу фильтровать обновления по типу и библиотекам, чтобы видеть только релевантные.
____

**Acceptance Criteria:**

[cols="1,3,3,3",options="header"]
|===
| ID | Given | When | Then

| AC-003-1
| 5 PATCH, 3 MINOR, 2 MAJOR обновлений
| Запускаю с `minUpdateType = MINOR`
| Показываются только 3 MINOR + 2 MAJOR (5 записей)

| AC-003-2
| Обновления для `kotlin-*` и `spring-*` библиотек
| Запускаю с `excludePatterns = ["org.springframework:*"]`
| Spring библиотеки исключены из результата

| AC-003-3
| Библиотека помечена как `ignoreUpdates = true` в DSL
| Запускаю проверку обновлений
| Библиотека пропускается
|===

== API использование

[source,kotlin]
----
val dependanger = Dependanger.fromDsl {
    // ... DSL config
}
.preset(ProcessingPreset.STRICT)  // STRICT включает update-check процессор
.build()

val result = dependanger.process()

// Получить информацию об обновлениях (через extraction extension)
val updates: List<UpdateAvailableInfo> = result.updates

updates.forEach { update ->
    println("$\{update.alias\}: $\{update.currentVersion\} -> $\{update.latestVersion\}")
}
----

== Типы обновлений

[cols="1,3,2",options="header"]
|===
| Тип | Описание | Пример

| PATCH
| Только patch версия изменилась
| 1.2.3 → 1.2.4

| MINOR
| Minor версия изменилась
| 1.2.3 → 1.3.0

| MAJOR
| Major версия изменилась
| 1.2.3 → 2.0.0

| PRERELEASE
| Есть preview/RC/beta версия
| 1.2.3 → 2.0.0-RC1
|===

== UpdateAvailableInfo

Информация об обновлении содержит:

* `library` — alias библиотеки
* `group`, `name` — Maven координаты
* `currentVersion` — текущая версия
* `latestStable` — последняя стабильная версия
* `latestAny` — последняя любая версия (включая RC)
* `updateType` — тип обновления (PATCH, MINOR, MAJOR, PRERELEASE)
* `repository` — репозиторий, где найдено обновление

== Конфигурация

=== Builder API

[source,kotlin]
----
// Включить через preset
Dependanger.fromDsl { /* ... */ }
    .preset(ProcessingPreset.STRICT)  // STRICT включает update-check
    .build()

// Конфигурация в DSL
settings {
    updateCheck {
        includePrerelease = false
        repositories = listOf(
            "https://repo.maven.apache.org/maven2",
            "https://maven.google.com"
        )
    }
}
----

=== Флаг ignoreUpdates

Для исключения конкретной библиотеки из проверки обновлений:

[source,kotlin]
----
dependanger {
    libraries {
        // Эта библиотека пропускается при проверке обновлений
        library("legacy-lib", "com.example:legacy:1.0.0") {
            ignoreUpdates = true
        }
    }
}
----

== SPI: UpdateCheckProcessor

`UpdateCheckProcessor` — опциональный процессор (order: 70, phase: PARALLEL_IO), который:

1. Для каждой библиотеки в effective metadata
2. Параллельно запрашивает доступные версии из Maven repositories
3. Сравнивает с текущей версией
4. Добавляет `UpdateAvailableInfo` в diagnostics

Включается через `withUpdateCheck()` в builder API.

== Кеширование

* Результаты кешируются в `~/.dependanger/cache/versions/`
* TTL: 1 час (настраивается)
* При отсутствии сети используется кеш

== CLI Command

=== dependanger check-updates

Проверка доступных обновлений через CLI.

[source,bash]
----
dependanger check-updates [options] <input>
----

**Аргументы:**

[cols="1,2,1",options="header"]
|===
| Аргумент | Описание | Default

| `<input>`
| Путь к `effective-*.json` файлу
| `effective-metadata.json`
|===

**Опции:**

[cols="1,2,1",options="header"]
|===
| Опция | Описание | Default

| `--output`, `-o`
| Путь для выходного файла (для --format file)
| `./updates.json`

| `--format`, `-f`
| Формат вывода: `text`, `json`, `file`
| `text`

| `--include-prerelease`
| Включать RC/beta/alpha версии
| `false`

| `--min-update-type`
| Минимальный тип: `patch`, `minor`, `major`
| `patch`

| `--exclude-patterns`
| Исключить библиотеки по паттерну (через запятую)
| —

| `--repositories`
| Репозитории для проверки (через запятую)
| Maven Central

| `--timeout`
| Таймаут на запрос (например, `30s`)
| `30s`

| `--cache-ttl`
| TTL кеша версий (например, `1h`)
| `1h`

| `--offline`
| Использовать только кеш
| `false`

| `--fail-on`
| Exit code 1 при: `any`, `major`, `minor`, `none`
| `none`

| `--parallel`
| Максимум параллельных запросов
| `10`
|===

**Примеры:**

[source,bash]
----
# Базовая проверка
dependanger check-updates effective-metadata.json

# JSON формат
dependanger check-updates --format json effective-metadata.json

# Сохранение в файл
dependanger check-updates --format file --output updates.json effective-metadata.json

# Только MINOR и MAJOR
dependanger check-updates --min-update-type minor effective-metadata.json

# Включая prereleases
dependanger check-updates --include-prerelease effective-metadata.json

# Fail на MAJOR обновлениях (для CI)
dependanger check-updates --fail-on major effective-metadata.json

# Исключение определённых библиотек
dependanger check-updates --exclude-patterns "org.springframework:*,io.ktor:*" effective-metadata.json
----

**Пример вывода (text):**

[source]
----
Checking updates for 15 libraries...

MAJOR updates:
  ktor-core: 2.3.0 -> 3.0.0

MINOR updates:
  spring-boot-starter: 3.2.0 -> 3.4.0
  kotlin-stdlib: 2.1.0 -> 2.1.20

PATCH updates:
  guava: 33.0.0-jre -> 33.0.1-jre

Summary: 1 major, 2 minor, 1 patch updates available
----

**Пример вывода (json):**

[source,json]
----
{
  "updates": [
    {
      "library": "ktor-core",
      "group": "io.ktor",
      "name": "ktor-core",
      "currentVersion": "2.3.0",
      "latestStable": "3.0.0",
      "updateType": "MAJOR",
      "repository": "https://repo.maven.apache.org/maven2"
    }
  ],
  "summary": {
    "total": 4,
    "major": 1,
    "minor": 2,
    "patch": 1
  }
}
----

== Gradle Task

=== dependangerCheckUpdates

Задача проверки обновлений в Gradle.

**Зависимости:**

* `dependangerGenerateEffective` — требуется EffectiveMetadata

**Inputs:**

* `effective-\{distribution\}.json` от `dependangerGenerateEffective`

**Outputs:**

* `build/dependanger/updates.json` — JSON с результатами
* Console output — текстовый отчёт

**Опции:**

* `includePrerelease` — включать RC/beta/alpha
* `minUpdateType` — минимальный тип обновления (`PATCH`, `MINOR`, `MAJOR`)
* `excludePatterns` — паттерны для исключения
* `failOnMajorUpdates` — exit code 1 при MAJOR обновлениях
* `failOnAnyUpdates` — exit code 1 при любых обновлениях

[source,bash]
----
# Базовая проверка
./gradlew dependangerCheckUpdates

# С включением prereleases
./gradlew dependangerCheckUpdates -PincludePrerelease=true

# Fail на MAJOR
./gradlew dependangerCheckUpdates -PfailOnMajorUpdates=true
----

== Settings DSL

Полная конфигурация проверки обновлений в Gradle DSL.

[source,kotlin]
----
dependanger {
    settings {
        updateCheck {
            // Включение процессора
            enabled = true  // или через processing { checkUpdates = true }

            // Типы обновлений
            includePrerelease = false  // default
            minUpdateType = PATCH  // PATCH, MINOR, MAJOR

            // Фильтрация
            excludePatterns = listOf(
                "org.springframework:*",  // все Spring
                "*:*-internal"  // все internal артефакты
            )

            // Репозитории
            repositories {
                mavenCentral()
                google()
                maven("https://nexus.example.com/releases") {
                    credentials {
                        username = providers.environmentVariable("NEXUS_USER")
                        password = providers.environmentVariable("NEXUS_PASSWORD")
                    }
                }
            }

            // Кеширование
            cache {
                enabled = true
                ttl = 1.hours  // default
                directory = file("~/.dependanger/cache/versions")
            }

            // Параллелизм
            parallelism = 10  // max concurrent requests

            // Таймауты
            timeout = 30.seconds  // per-request timeout

            // CI интеграция
            failOnMajorUpdates = false  // default
            failOnMinorUpdates = false  // default
            failOnAnyUpdates = false  // default
        }
    }

    // Или короткая форма через processing
    processing {
        checkUpdates = true
    }
}
----

== Функциональные требования

[cols="1,1,4",options="header"]
|===
| ID | Приоритет | Требование

| FR-001
| MUST
| Система ДОЛЖНА запрашивать доступные версии из Maven репозиториев

| FR-002
| MUST
| Система ДОЛЖНА классифицировать обновления по типу (PATCH, MINOR, MAJOR, PRERELEASE)

| FR-003
| MUST
| Система ДОЛЖНА добавлять результаты в `Diagnostics`

| FR-004
| MUST
| Система ДОЛЖНА поддерживать параллельные запросы к репозиториям (PARALLEL_IO phase)

| FR-005
| SHOULD
| Система ДОЛЖНА кешировать результаты запросов к репозиториям

| FR-006
| SHOULD
| Система ДОЛЖНА использовать кеш при недоступности сети

| FR-007
| SHOULD
| Система ДОЛЖНА поддерживать настройку списка репозиториев

| FR-008
| SHOULD
| Система ДОЛЖНА поддерживать таймаут на запросы

| FR-009
| SHOULD
| Система ДОЛЖНА поддерживать фильтрацию по типу обновления (minUpdateType)

| FR-010
| MAY
| Система МОЖЕТ поддерживать исключение библиотек по паттерну

| FR-011
| MAY
| Система МОЖЕТ поддерживать флаг `ignoreUpdates` на уровне библиотеки

| FR-012
| MAY
| Система МОЖЕТ определять security-критичные обновления (через OSV API)

| FR-013
| MUST
| Система ДОЛЖНА поддерживать CLI команду `dependanger check-updates`

| FR-014
| SHOULD
| Система ДОЛЖНА поддерживать форматы вывода: `text`, `json`, `file`

| FR-015
| SHOULD
| Система ДОЛЖНА поддерживать опцию `--fail-on` для интеграции в CI

| FR-016
| SHOULD
| Система ДОЛЖНА выводить summary статистику (total, major, minor, patch)
|===

== Edge Cases

[cols="1,3,2",options="header"]
|===
| Сценарий | Описание | Ожидаемое поведение

| Репозиторий недоступен
| Maven Central не отвечает, кеш пуст
| WARNING: "Cannot reach repository", библиотека пропускается

| Репозиторий недоступен, есть кеш
| Maven Central не отвечает, есть кеш < TTL
| Используется кеш, WARNING в логе

| Таймаут запроса
| Репозиторий отвечает > timeout (30s default)
| WARNING: "Request timeout for library 'xxx'", библиотека пропускается

| Библиотека не найдена
| Библиотека с координатами которых нет в репозиториях
| WARNING: "Library 'xxx' not found in any repository"

| Невалидный формат версии
| Текущая версия не соответствует SemVer: `1.0.Final`
| Попытка парсинга, при неудаче — WARNING, сравнение как строки

| Версия с qualifier
| Версия `1.0.0.RELEASE` vs `1.0.1`
| Корректное сравнение с учётом qualifier

| Много версий в репозитории
| Библиотека имеет 500+ версий
| Запрашиваются только последние N версий (оптимизация)

| Пустой каталог
| EffectiveMetadata без библиотек
| Процессор завершается успешно, 0 обновлений

| Все библиотеки актуальны
| Нет доступных обновлений
| Пустой список UpdateAvailableInfo, INFO: "All libraries are up to date"

| Prerelease текущая версия
| Текущая версия `2.0.0-RC1`, есть стабильная `2.0.0`
| Показывается обновление до `2.0.0` (стабильная > prerelease)

| Downgrade
| Текущая версия `2.0.0`, в репозитории максимум `1.9.0`
| Не показывается как обновление (не downgrade)

| Snapshot версия
| Текущая версия `1.0.0-SNAPSHOT`
| WARNING: "Snapshot version for 'xxx'", проверяется на обновления

| Private репозиторий
| Репозиторий требует аутентификации
| Поддержка через `repositories { maven { url; credentials { } } }`

| Rate limiting
| Репозиторий возвращает 429 Too Many Requests
| Exponential backoff, WARNING после N попыток

| Параллельные запросы
| 100 библиотек для проверки
| Запросы выполняются параллельно с ограничением (max 10 concurrent)
|===

== Связи

* **Входные данные:** `EffectiveMetadata` с разрешёнными версиями
* **Выходные данные:** `UpdateAvailableInfo` в diagnostics
* **Позиция в pipeline:** order 70 (после Validation)

== Связанные документы

* xref:F003-processing-pipeline.adoc[F003: Processing Pipeline] — место в pipeline (order: 70)
* xref:F009-report-generation.adoc[F009: Report Generation] — включает update info
* xref:../03-interfaces/cli.adoc[CLI — команда check-updates]
* xref:../03-interfaces/gradle-plugin.adoc[Gradle Plugin — задача dependangerCheckUpdates]
* xref:F010-security-scanning.adoc[F010: Security Scanning] — security-критичные обновления
