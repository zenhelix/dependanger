= F002: Metadata Serialization
:toc: left
:toclevels: 3
:sectnums:
:icons: font

[abstract]
--
Сериализация модели `DependangerMetadata` в промежуточный формат (JSON) через расширяемый SPI.
--

== Метаданные

[cols="1,2"]
|===
| ID | F002
| Название | Metadata Serialization
| Приоритет | P1 (критично для MVP)
| Статус | 🟢 Готов
| Модуль | `dependanger-metadata-json` (+ SPI в core)
| Зависит от | F001 (Registry DSL)
| Блокирует | F003 (Processing Pipeline)
|===

== Описание

Metadata Serialization преобразует `DependangerDsl` (модель в памяти) в сериализуемый `DependangerMetadata` для:
* Хранения между запусками * Передачи между этапами pipeline * Кеширования для инкрементальной обработки

== User Stories

=== US-001: Сохранение метаданных в JSON

[quote]
____
Как пользователь Gradle plugin, я хочу чтобы результат DSL сохранялся в файл, чтобы другие таски могли использовать его без повторного парсинга DSL.
____

**Acceptance Criteria:**
* DSL конвертируется в `metadata.json`
* JSON содержит все данные из DependangerMetadata * Файл читается и десериализуется обратно в модель

=== US-002: Расширение форматами

[quote]
____
Как продвинутый пользователь, я хочу использовать YAML вместо JSON, чтобы метаданные были более читаемыми.
____

**Acceptance Criteria:**
* Формат определяется через конфигурацию * Можно добавить кастомный формат через SPI * Все форматы поддерживают одинаковую модель данных

== SPI: SerializationFormat

=== Описание интерфейса

Интерфейс `SerializationFormat<T>` предоставляет точку расширения для добавления новых форматов сериализации.

**Основные возможности:**

* `formatId` — уникальный идентификатор формата (например: "json", "yaml")
* `fileExtension` — расширение файла (например: ".json", ".yaml")
* `serialize()` / `deserialize()` — преобразование модели в формат и обратно
* `write()` / `read()` — работа с файловой системой

=== Реализации

[cols="1,2,2",options="header"]
|===
| Format ID | Модуль | Технология

| `json`
| `dependanger-metadata-json`
| kotlinx.serialization JSON

| `yaml`
| (планируется)
| kaml

| `toml`
| (планируется)
| ktoml
|===

== Модель сериализации

=== DependangerMetadata

Корневая структура модели сериализации содержит:

* `schemaVersion` — версия схемы для обратной совместимости
* `versions` — именованные версии
* `libraries` — библиотеки с метаданными
* `plugins` — Gradle plugins
* `bundles` — группы библиотек
* `platforms` — целевые платформы и BOM imports
* `distributions` — профили фильтрации
* `compatibility` — правила совместимости
* `presets` — именованные presets настроек
* `settings` — глобальные настройки

=== Вложенные структуры

[cols="1,2",options="header"]
|===
| Структура | Описание

| `VersionMetadata`
| Версия с value, deprecation статусом и fallback условиями

| `LibraryMetadata`
| Maven координаты, теги, требования к платформе, поддерживаемые targets

| `PluginMetadata`
| Gradle plugin ID и версия

| `BundleMetadata`
| Список библиотек и включаемых бандлов

| `PlatformMetadata`
| Constraints для JDK, Android, поддерживаемые Kotlin targets

| `DistributionMetadata`
| Spec фильтрации, publishing настройки

| `Preset`
| Именованный preset с предустановленными настройками

| `SettingsMetadata`
| Глобальные настройки валидации
|===

=== Пример JSON

[source,json]
----
{
  "schemaVersion": "1.0",
  "versions": {
    "kotlin": {
      "value": "2.1.20",
      "fallbacks": [
        { "value": "1.9.24", "condition": { "type": "jdkBelow", "version": 17 } }
      ]
    },
    "spring-boot": {
      "value": "3.4.0"
    }
  },
  "libraries": {
    "kotlin-stdlib": {
      "group": "org.jetbrains.kotlin",
      "artifact": "kotlin-stdlib",
      "versionRef": "kotlin",
      "tags": ["kotlin", "core"]
    }
  },
  "plugins": {
    "kotlin-jvm": {
      "id": "org.jetbrains.kotlin.jvm",
      "versionRef": "kotlin"
    }
  },
  "bundles": {
    "kotlin-core": {
      "libraries": ["kotlin-stdlib", "kotlin-reflect"]
    }
  },
  "platforms": {},
  "distributions": {},
  "compatibility": [],
  "presets": {},
  "settings": {}
}
----

== Gradle Task

=== dependangerGenerateMetadata

Задача `dependangerGenerateMetadata` сериализует DSL модель в файл.

**Inputs:**

* `DependangerMetadata` из DSL extension

**Outputs:**

* `build/dependanger/metadata.json`

== Версионирование схемы

[cols="1,2",options="header"]
|===
| Версия | Изменения

| 1.0
| Начальная версия

| 1.1
| (планируется) Добавлены kmpTargets в Library
|===

При десериализации:
* Проверяется `schemaVersion`
* Поддерживается миграция между версиями * Неизвестные поля игнорируются (forward compatibility)

== Связи

* **Входные данные:** `DependangerMetadata` от xref:F001-registry-dsl.adoc[F001]
* **Выходные данные:** `metadata.json` (или другой формат)
* **Следующий этап:** xref:F003-processing-pipeline.adoc[F003: Processing Pipeline]

== Связанные документы

* xref:../01-concepts/domain-model.adoc[Доменная модель — Metadata Model]
* xref:../01-concepts/architecture.adoc[Архитектура — SerializationFormat SPI]
