= SPEC-09: dependanger-report -- Реализация TODO-заглушек
:toc: left
:toclevels: 3
:sectnums:

[[overview]]
== Обзор

Модуль `dependanger-report` генерирует единый отчёт о состоянии зависимостей, объединяя данные из EffectiveMetadata и extensions (updates, vulnerabilities, licenses, compatibility, transitives).
Поддерживает форматы: JSON, YAML, Markdown, HTML.

NOTE: `ReportGenerator` реализует SPI `ReportProvider` (из `io.github.zenhelix.dependanger.effective.spi`) и регистрируется через ServiceLoader.
Он НЕ является `EffectiveMetadataProcessor` и не участвует в pipeline напрямую.
Вызывается через SPI discovery из CLI (`report` command), Gradle (`dependangerReport` task) и API после завершения pipeline.
В `ProcessingPhase` нет фазы REPORT.

Приоритет: **P3** (желательно).

[[report-models]]
== Модели отчёта

=== Enums

[source,kotlin]
----
@Serializable
public enum class ReportFormat { JSON, YAML, MARKDOWN, HTML }

@Serializable
public enum class ReportSection {
    SUMMARY, LIBRARIES, PLUGINS, BUNDLES, VERSIONS, UPDATES, COMPATIBILITY,
    VULNERABILITIES, DEPRECATED, LICENSES, TRANSITIVES, CONSTRAINTS, VALIDATION
}
----

=== Report Entity Models

[source,kotlin]
----
@Serializable
public data class ReportLibrary(
    val alias: String,
    val group: String,
    val artifact: String,
    val version: String?,
    val tags: Set<String>,
    val isDeprecated: Boolean,
    val isPlatform: Boolean,
)

@Serializable
public data class ReportPlugin(
    val alias: String,
    val id: String,
    val version: String?,
)

@Serializable
public data class ReportBundle(
    val alias: String,
    val libraries: List<String>,
)

@Serializable
public data class ReportDeprecated(
    val alias: String,
    val reason: String?,
    val replacement: String?,
)

@Serializable
public data class ReportUpdate(
    val alias: String,
    val currentVersion: String,
    val availableVersion: String,
    val type: String, // MAJOR, MINOR, PATCH
)

@Serializable
public data class ReportCompatibilityIssue(
    val ruleId: String,
    val severity: String,
    val message: String,
    val suggestion: String?,
)

@Serializable
public data class ReportVulnerability(
    val id: String,
    val cveId: String? = null,
    val library: String,
    val severity: String,
    val cvssScore: Double? = null,
    val fixedVersion: String? = null,
    val summary: String,
)

@Serializable
public data class ReportLicense(
    val library: String,
    val licenseName: String,
    val status: String, // ALLOWED, DENIED, UNKNOWN
)

@Serializable
public data class ReportVersion(
    val alias: String,
    val value: String,
    val hasFallback: Boolean,
)

@Serializable
public data class ReportTransitives(
    val directCount: Int,
    val transitiveCount: Int,
    val conflictCount: Int,
    val conflicts: List<ReportConflict>,
)

@Serializable
public data class ReportConflict(
    val artifact: String,
    val versions: List<String>,
)

@Serializable
public data class ReportConstraint(
    val type: String,
    val description: String,
)

@Serializable
public data class ReportValidation(
    val errors: List<ReportDiagnostic>,
    val warnings: List<ReportDiagnostic>,
)

@Serializable
public data class ReportDiagnostic(
    val code: String,
    val message: String,
)
----

[[spec-report-generator]]
== SPEC-09.1: ReportGenerator.generate()

[[purpose-report]]
=== Назначение

Собрать данные из `EffectiveMetadata` (библиотеки, плагины, бандлы) и extensions (updates, vulnerabilities, licenses, compatibility, transitives), сформировать отчёт в запрошенном формате.

[[algorithm-report]]
=== Алгоритм

[source,text]
----
1. Построить ReportData из EffectiveMetadata:
   a. summary -> подсчитать счётчики
   b. libraries -> из metadata.libraries
   c. plugins -> из metadata.plugins
   d. bundles -> из metadata.bundles
   e. updates -> из metadata.updates (extension)
   f. compatibility -> из metadata.compatibilityIssues (extension)
   g. vulnerabilities -> из metadata.vulnerabilities (extension)
   h. deprecated -> отфильтровать библиотеки с isDeprecated=true
   i. licenses -> из extensions (LicenseResult для каждой библиотеки)
   j. transitives -> из metadata.transitives (extension)
   k. constraints -> из originalMetadata?.constraints (если передан)
   l. validation -> из metadata.diagnostics (errors + warnings)
2. Отфильтровать секции по списку `sections`
3. Построить summary:
   - libraryCount, pluginCount, bundleCount
   - deprecatedCount -> count isDeprecated
   - updatesAvailable -> updates.size
   - vulnerabilitiesCount, criticalVulnerabilities, highVulnerabilities
   - deniedLicenses, unknownLicenses
   - compatibilityIssues
   - validationErrors, validationWarnings
4. Сериализовать в формат:
   a. JSON -> kotlinx-serialization Json.encodeToString с prettyPrint
   b. YAML -> kaml (kotlinx-serialization YAML): Yaml.default.encodeToString(data)
   c. Markdown -> шаблонный рендеринг (buildString, все 13 секций)
   d. HTML -> Markdown -> commonmark-java Parser+HtmlRenderer -> HTML template с CSS
5. `generate()` возвращает `GeneratedReport(format, content, outputPath = null)` -- без записи в файл
6. Запись в файл через отдельный метод `generateToFile()` (atomic write через temp файл)
----

[[implementation-report]]
=== Детальная реализация

==== ReportData (внутренняя модель)

[source,kotlin]
----
@Serializable
internal data class ReportData(
    val schemaVersion: String,
    val generatedAt: String, // ISO 8601 UTC
    val distribution: String?,
    val summary: ReportSummary,
    val libraries: List<ReportLibrary>?,
    val plugins: List<ReportPlugin>?,
    val bundles: List<ReportBundle>?,
    val versions: List<ReportVersion>?,
    val updates: List<ReportUpdate>?,
    val compatibility: List<ReportCompatibilityIssue>?,
    val vulnerabilities: List<ReportVulnerability>?,
    val deprecated: List<ReportDeprecated>?,
    val licenses: List<ReportLicense>?,
    val transitives: ReportTransitives?,
    val constraints: List<ReportConstraint>?,
    val validation: ReportValidation?,
)

@Serializable
internal data class ReportSummary(
    val libraryCount: Int,
    val pluginCount: Int,
    val bundleCount: Int,
    val deprecatedCount: Int,
    val updatesAvailable: Int,
    val vulnerabilitiesCount: Int,
    val criticalVulnerabilities: Int,
    val highVulnerabilities: Int,
    val deniedLicenses: Int,
    val unknownLicenses: Int,
    val compatibilityIssues: Int,
    val validationErrors: Int,
    val validationWarnings: Int,
)
----

==== ReportGenerator (implements ReportProvider SPI)

`GeneratedReport` определён в `io.github.zenhelix.dependanger.effective.spi.ReportProvider`:

[source,kotlin]
----
public data class GeneratedReport(
    val format: ReportFormat,
    val content: String,
    val outputPath: Path?,
)
----

[source,kotlin]
----
public class ReportGenerator : ReportProvider {

    override val providerId: String = "report"

    private val renderers: Map<ReportFormat, ReportRenderer> = mapOf(
        ReportFormat.JSON to JsonReportRenderer(),
        ReportFormat.YAML to YamlReportRenderer(),
        ReportFormat.MARKDOWN to MarkdownReportRenderer(),
        ReportFormat.HTML to HtmlReportRenderer(markdownRenderer = MarkdownReportRenderer()),
    )

    /**
     * Генерация отчёта без записи в файл (outputPath = null).
     */
    override fun generate(
        effective: EffectiveMetadata,
        settings: ReportSettings,
        originalMetadata: DependangerMetadata?,
    ): GeneratedReport {
        val reportData = ReportDataMapper.buildReportData(
            effective = effective,
            sections = settings.sections,
            originalMetadata = originalMetadata,
            summaryOnly = false,
        )

        val renderer = renderers.getValue(settings.format)
        val content = renderer.render(reportData)

        return GeneratedReport(
            format = settings.format,
            content = content,
            outputPath = null,
        )
    }

    /**
     * Генерация + запись в файл (atomic write через temp файл).
     */
    override fun generateToFile(
        effective: EffectiveMetadata,
        settings: ReportSettings,
        originalMetadata: DependangerMetadata?,
    ): GeneratedReport {
        val report = generate(effective, settings, originalMetadata)
        // Atomic write: создать temp файл, записать, переименовать в target
        val outputPath = Path.of(settings.outputDir).resolve("dependanger-report.${formatExtension(settings.format)}")
        // ...
        return report.copy(outputPath = outputPath)
    }
}
----

==== Renderer архитектура

Sealed interface `ReportRenderer` + 4 отдельных класса-реализации:

[source,kotlin]
----
internal sealed interface ReportRenderer {
    fun render(data: ReportData): String
}

internal class JsonReportRenderer : ReportRenderer { ... }
internal class YamlReportRenderer : ReportRenderer { ... }
internal class MarkdownReportRenderer : ReportRenderer { ... }
internal class HtmlReportRenderer(markdownRenderer: MarkdownReportRenderer) : ReportRenderer { ... }
----

==== JSON рендеринг (JsonReportRenderer)

[source,kotlin]
----
private val json = Json {
    prettyPrint = true
    encodeDefaults = false
    explicitNulls = false
}

override fun render(data: ReportData): String =
    json.encodeToString(data)
----

==== YAML рендеринг (YamlReportRenderer)

Используется библиотека https://github.com/charleskorn/kaml[kaml] -- нативная kotlinx-serialization поддержка YAML.
`ReportData` уже `@Serializable`, рендеринг -- одна строка.

[source,kotlin]
----
import com.charleskorn.kaml.Yaml

override fun render(data: ReportData): String =
    Yaml.default.encodeToString(ReportData.serializer(), data)
----

==== Markdown рендеринг

Полный шаблонный рендеринг всех 13 секций отчёта.

[source,kotlin]
----
private fun renderMarkdown(data: ReportData): String = buildString {
    appendLine("# Dependanger Report")
    appendLine()
    appendLine("Generated: ${data.generatedAt}")
    data.distribution?.let { appendLine("Distribution: $it") }
    appendLine()

    // Summary (всегда)
    appendLine("## Summary")
    appendLine()
    appendLine("| Metric | Count |")
    appendLine("|--------|-------|")
    appendLine("| Libraries | ${data.summary.libraryCount} |")
    appendLine("| Plugins | ${data.summary.pluginCount} |")
    appendLine("| Bundles | ${data.summary.bundleCount} |")
    if (data.summary.deprecatedCount > 0)
        appendLine("| Deprecated | ${data.summary.deprecatedCount} |")
    if (data.summary.updatesAvailable > 0)
        appendLine("| Updates Available | ${data.summary.updatesAvailable} |")
    if (data.summary.vulnerabilitiesCount > 0) {
        appendLine("| Vulnerabilities | ${data.summary.vulnerabilitiesCount} |")
        appendLine("| Critical | ${data.summary.criticalVulnerabilities} |")
        appendLine("| High | ${data.summary.highVulnerabilities} |")
    }
    if (data.summary.deniedLicenses > 0)
        appendLine("| Denied Licenses | ${data.summary.deniedLicenses} |")
    if (data.summary.compatibilityIssues > 0)
        appendLine("| Compatibility Issues | ${data.summary.compatibilityIssues} |")
    if (data.summary.validationErrors > 0)
        appendLine("| Validation Errors | ${data.summary.validationErrors} |")
    if (data.summary.validationWarnings > 0)
        appendLine("| Validation Warnings | ${data.summary.validationWarnings} |")

    // Libraries
    data.libraries?.let { libs ->
        appendLine()
        appendLine("## Libraries (${libs.size})")
        appendLine()
        appendLine("| Alias | Coordinates | Version | Tags |")
        appendLine("|-------|------------|---------|------|")
        for (lib in libs) {
            val tags = lib.tags.joinToString(", ").ifEmpty { "-" }
            appendLine("| ${lib.alias} | ${lib.group}:${lib.artifact} | ${lib.version ?: "N/A"} | $tags |")
        }
    }

    // Plugins
    data.plugins?.let { plugins ->
        appendLine()
        appendLine("## Plugins (${plugins.size})")
        appendLine()
        appendLine("| Alias | Plugin ID | Version |")
        appendLine("|-------|-----------|---------|")
        for (p in plugins) {
            appendLine("| ${p.alias} | ${p.id} | ${p.version ?: "N/A"} |")
        }
    }

    // Bundles
    data.bundles?.let { bundles ->
        appendLine()
        appendLine("## Bundles (${bundles.size})")
        appendLine()
        for (b in bundles) {
            appendLine("- **${b.alias}**: ${b.libraries.joinToString(", ")}")
        }
    }

    // Versions
    data.versions?.let { versions ->
        appendLine()
        appendLine("## Versions (${versions.size})")
        appendLine()
        appendLine("| Alias | Value | Fallback |")
        appendLine("|-------|-------|----------|")
        for (v in versions) {
            val fallback = if (v.hasFallback) "Yes" else "-"
            appendLine("| ${v.alias} | ${v.value} | $fallback |")
        }
    }

    // Deprecated
    data.deprecated?.let { deprecated ->
        appendLine()
        appendLine("## Deprecated Libraries (${deprecated.size})")
        appendLine()
        appendLine("| Library | Reason | Replacement |")
        appendLine("|---------|--------|-------------|")
        for (d in deprecated) {
            appendLine("| ${d.alias} | ${d.reason ?: "-"} | ${d.replacement ?: "-"} |")
        }
    }

    // Updates
    data.updates?.let { updates ->
        appendLine()
        appendLine("## Available Updates (${updates.size})")
        appendLine()
        appendLine("| Library | Current | Available | Type |")
        appendLine("|---------|---------|-----------|------|")
        for (u in updates) {
            appendLine("| ${u.alias} | ${u.currentVersion} | ${u.availableVersion} | ${u.type} |")
        }
    }

    // Compatibility Issues
    data.compatibility?.let { issues ->
        appendLine()
        appendLine("## Compatibility Issues (${issues.size})")
        appendLine()
        appendLine("| Rule | Severity | Message | Suggestion |")
        appendLine("|------|----------|---------|------------|")
        for (issue in issues) {
            appendLine("| ${issue.ruleId} | ${issue.severity} | ${issue.message} | ${issue.suggestion ?: "-"} |")
        }
    }

    // Vulnerabilities
    data.vulnerabilities?.let { vulns ->
        appendLine()
        appendLine("## Vulnerabilities (${vulns.size})")
        appendLine()
        appendLine("| Library | CVE | Severity | CVSS | Fixed In | Summary |")
        appendLine("|---------|-----|----------|------|----------|---------|")
        for (v in vulns) {
            val cve = v.cveId ?: v.id
            val cvss = v.cvssScore?.let { "%.1f".format(it) } ?: "-"
            appendLine("| ${v.library} | $cve | ${v.severity} | $cvss | ${v.fixedVersion ?: "-"} | ${v.summary} |")
        }
    }

    // Licenses
    data.licenses?.let { licenses ->
        appendLine()
        appendLine("## Licenses (${licenses.size})")
        appendLine()
        appendLine("| Library | License | Status |")
        appendLine("|---------|---------|--------|")
        for (l in licenses) {
            appendLine("| ${l.library} | ${l.licenseName} | ${l.status} |")
        }
    }

    // Transitives
    data.transitives?.let { transitives ->
        appendLine()
        appendLine("## Transitive Dependencies")
        appendLine()
        appendLine("- Direct: ${transitives.directCount}")
        appendLine("- Transitive: ${transitives.transitiveCount}")
        appendLine("- Conflicts: ${transitives.conflictCount}")
        if (transitives.conflicts.isNotEmpty()) {
            appendLine()
            appendLine("### Version Conflicts")
            appendLine()
            appendLine("| Artifact | Versions |")
            appendLine("|----------|----------|")
            for (c in transitives.conflicts) {
                appendLine("| ${c.artifact} | ${c.versions.joinToString(" vs ")} |")
            }
        }
    }

    // Constraints
    data.constraints?.let { constraints ->
        appendLine()
        appendLine("## Constraints (${constraints.size})")
        appendLine()
        for (c in constraints) {
            appendLine("- **${c.type}**: ${c.description}")
        }
    }

    // Validation
    data.validation?.let { validation ->
        appendLine()
        appendLine("## Validation")
        appendLine()
        if (validation.errors.isNotEmpty()) {
            appendLine("### Errors (${validation.errors.size})")
            appendLine()
            for (e in validation.errors) { appendLine("- ❌ [${e.code}] ${e.message}") }
        }
        if (validation.warnings.isNotEmpty()) {
            appendLine("### Warnings (${validation.warnings.size})")
            appendLine()
            for (w in validation.warnings) { appendLine("- ⚠️ [${w.code}] ${w.message}") }
        }
        if (validation.errors.isEmpty() && validation.warnings.isEmpty()) {
            appendLine("All checks passed.")
        }
    }
}
----

==== HTML рендеринг

Используется библиотека https://github.com/commonmark/commonmark-java[commonmark-java] для конвертации Markdown -> HTML.
Результат оборачивается в HTML-шаблон с базовым CSS.

[source,kotlin]
----
import org.commonmark.parser.Parser
import org.commonmark.renderer.html.HtmlRenderer

private val mdParser = Parser.builder().build()
private val htmlRenderer = HtmlRenderer.builder().build()

private fun renderHtml(data: ReportData): String {
    val markdown = renderMarkdown(data)
    val document = mdParser.parse(markdown)
    val body = htmlRenderer.render(document)
    return htmlTemplate(data.generatedAt, body)
}

private fun htmlTemplate(generatedAt: String, body: String): String = """
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Dependanger Report</title>
<style>
  body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
         max-width: 960px; margin: 0 auto; padding: 24px; color: #333; line-height: 1.5; }
  h1 { border-bottom: 2px solid #2196F3; padding-bottom: 8px; }
  h2 { border-bottom: 1px solid #ddd; padding-bottom: 4px; margin-top: 32px; }
  table { border-collapse: collapse; width: 100%; margin: 12px 0; }
  th, td { border: 1px solid #ddd; padding: 8px 12px; text-align: left; }
  th { background: #f5f5f5; font-weight: 600; }
  tr:hover { background: #fafafa; }
  code { background: #f5f5f5; padding: 2px 6px; border-radius: 3px; font-size: 0.9em; }
  footer { margin-top: 40px; padding-top: 12px; border-top: 1px solid #ddd;
           font-size: 0.85em; color: #888; }
</style>
</head>
<body>
$body
<footer>Generated by Dependanger at $generatedAt</footer>
</body>
</html>
""".trimIndent()
----

[[report-sections]]
== Секции отчёта

[cols="1,2,2"]
|===
| Секция | Источник данных | Условие включения

| `summary` | Вычисляемая | Всегда
| `libraries` | `metadata.libraries` | ReportSection.LIBRARIES in sections
| `plugins` | `metadata.plugins` | ReportSection.PLUGINS in sections
| `bundles` | `metadata.bundles` | ReportSection.BUNDLES in sections
| `versions` | `metadata.versions` | ReportSection.VERSIONS in sections
| `updates` | `metadata.updates` (ExtensionKey) | ReportSection.UPDATES in sections + данные есть
| `compatibility` | `metadata.compatibilityIssues` | ReportSection.COMPATIBILITY in sections + данные есть
| `vulnerabilities` | `metadata.vulnerabilities` | ReportSection.VULNERABILITIES in sections + данные есть
| `deprecated` | `metadata.libraries.filter { isDeprecated }` | ReportSection.DEPRECATED in sections
| `licenses` | Extension из LicenseCheckProcessor | ReportSection.LICENSES in sections + данные есть
| `transitives` | `metadata.transitives` | ReportSection.TRANSITIVES in sections + данные есть
| `constraints` | `originalMetadata?.constraints` | ReportSection.CONSTRAINTS in sections + originalMetadata != null
| `validation` | `metadata.diagnostics` | ReportSection.VALIDATION in sections
|===

[[json-schema]]
== JSON Schema (версия 1.0)

Структура полностью описана в документации F009. Ключевые поля:

- `schemaVersion`: "1.0" -- обязательное
- `generatedAt`: ISO 8601 UTC -- обязательное
- `distribution`: String? -- optional
- `summary`: объект со счётчиками -- обязательное
- Остальные секции -- optional (null если процессор не запущен)

[[edge-cases]]
== Edge Cases и обработка ошибок

[cols="1,2,2"]
|===
| Ситуация | Поведение | Обоснование

| Пустой каталог | Валидный отчёт с пустыми массивами, summary с нулями | Корректный сценарий
| Процессор не запущен | Секция = null в JSON | Не путать с "нет данных" и "проверка не выполнена"
| summaryOnly | Только schemaVersion, generatedAt, distribution, summary | Для быстрого просмотра
| Специальные символы | JSON escaping: `"`, `\n`, unicode | Стандартное JSON поведение
| Очень большой каталог (1000+) | Генерация продолжается, WARNING при > 500KB | Рекомендуется --summary-only
| Нет прав на запись | ERROR "Cannot write report" | Fail-fast
| Файл уже существует | Перезаписать (atomic write через temp) | Идемпотентность
| Невалидный формат | Невозможно: ReportFormat -- sealed enum | Compile-time safety
| Timestamp | Всегда UTC: "2026-01-27T10:30:00Z" | Consistency
| Прерывание (Ctrl+C) | Частичный файл удаляется (atomic write) | Не оставлять мусор
|===

[[diagnostic-codes]]
== Диагностические коды

ReportGenerator не добавляет собственных диагностических кодов -- он только читает данные из metadata и extensions.

[[extension-keys]]
== ExtensionKey

ReportGenerator читает из extensions:

[source,kotlin]
----
metadata.updates                // UpdatesExtensionKey
metadata.vulnerabilities        // VulnerabilitiesExtensionKey
metadata.compatibilityIssues    // CompatibilityIssuesExtensionKey
metadata.licenseViolations      // LicenseViolationsExtensionKey
metadata.transitives            // TransitivesExtensionKey
metadata.flatDependencies       // FlatDependenciesExtensionKey
metadata.versionConflicts       // VersionConflictsExtensionKey
----

[[testing]]
== Тестовые сценарии

[cols="1,3"]
|===
| Тест | Описание

| `empty metadata produces valid JSON` | Пустой каталог -> валидный JSON с нулевыми счётчиками
| `libraries included in report` | 10 библиотек -> JSON содержит 10 записей
| `updates section from extension` | UpdateAvailableInfo -> отчёт содержит updates
| `vulnerabilities section from extension` | VulnerabilityInfo -> отчёт содержит vulnerabilities
| `compatibility section from extension` | CompatibilityIssue -> отчёт содержит compatibility
| `deprecated libraries filtered` | isDeprecated=true -> deprecated секция
| `summary counts are correct` | Все счётчики соответствуют данным
| `section filtering works` | sections=[SUMMARY, LIBRARIES] -> только эти секции
| `summaryOnly mode` | Только summary + metadata полей
| `JSON format is valid` | Парсинг через kotlinx.serialization
| `YAML format is valid` | Парсинг через kaml Yaml.default.decodeFromString
| `Markdown format renders all sections` | Markdown содержит все 13 секций с таблицами и заголовками
| `Markdown summary shows non-zero metrics only` | Нулевые метрики не показываются в summary
| `HTML format is valid` | Содержит DOCTYPE, head, body, CSS
| `HTML contains rendered tables` | Markdown таблицы -> HTML <table> через commonmark
| `null sections when processor not run` | updates=null если withUpdateCheck() не вызывался
| `atomic write creates file` | Файл создан по указанному пути
| `file overwrite works` | Перезапись существующего файла
| `timestamp is UTC ISO 8601` | generatedAt формат корректен
|===

[[dependencies]]
== Зависимости модуля

[source,text]
----
dependanger-report зависит от:
- dependanger-core (api)
- dependanger-effective (api)
- org.jetbrains.kotlinx:kotlinx-serialization-json:1.8.0 (implementation)
- com.charleskorn.kaml:kaml:0.67.0 (implementation) -- YAML рендеринг (kotlinx-serialization native)
- org.commonmark:commonmark:0.24.0 (implementation) -- Markdown -> HTML конвертация
- io.github.oshai:kotlin-logging-jvm:7.0.3 (implementation)
----

[[files-to-modify]]
== Файлы для изменения/создания

[cols="1,2"]
|===
| Файл | Изменение

| `ReportGenerator.kt` | Реализация `ReportProvider` SPI -- `generate()` (возвращает `GeneratedReport` с `outputPath = null`) и `generateToFile()` (atomic write)
| `ReportData.kt` | **Создать** -- внутренние @Serializable модели: ReportData, ReportSummary (поля без default values)
| `ReportModels.kt` | **Создать** -- публичные @Serializable модели секций (ReportUpdate, ReportVulnerability, etc.), поля без default values
| `ReportRenderer.kt` | **Создать** -- sealed interface `ReportRenderer` + 4 отдельных класса: `JsonReportRenderer`, `YamlReportRenderer`, `MarkdownReportRenderer`, `HtmlReportRenderer`
| `ReportDataMapper.kt` | **Создать** -- маппинг `EffectiveMetadata` -> `ReportData`
|===

[[priority]]
== Приоритет

**P3** (желательно) -- отчёт является ключевым артефактом для CI/CD интеграции и дашбордов.
Schema version 1.0 обеспечивает forward/backward compatibility.
