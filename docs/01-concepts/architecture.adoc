= Архитектура системы
:toc: left
:toclevels: 3
:sectnums:
:icons: font

[abstract]
--
Документ описывает архитектуру системы Dependanger:
архитектурные слои, модули, потоки данных и ключевые SPI интерфейсы.

Системный контекст: xref:../00-overview.adoc#system-context[Overview → Контекст].
--

== Принципы архитектуры

[cols="1,3",options="header"]
|===
| Принцип | Описание

| Направление зависимостей
| Зависимости направлены от периферии к ядру: interfaces → api → features → core

| Изоляция домена
| Core модуль не зависит от инфраструктуры (HTTP, файловая система, форматы)

| Расширяемость через SPI
| Новый функционал добавляется через реализацию интерфейсов, без изменения core

| Единая точка входа
| Весь функционал доступен через API фасад, интерфейсы (CLI, Gradle) — тонкие обёртки

| Абстракция форматов
| Сериализация модели абстрагирована через `SerializationFormat<T>` SPI

| Расширяемая генерация
| Генераторы артефактов добавляются через `ArtifactGenerator<T>` SPI

| Экспортный слой на границе
| Генераторы — точка экспорта. Артефакты (TOML, BOM) публикуются для внешних проектов
|===

== Архитектурные слои

Система организована в 4 архитектурных слоя с дополнительными SPI-реализациями:

[mermaid,target=architecture-layers,format=svg]
----
flowchart TB
    subgraph L1["1. INTERFACES"]
        I1["CLI, Gradle Plugin"]
    end
    subgraph L2["2. API"]
        A1["Фасад Dependanger, координация"]
    end
    subgraph L3["3. MODEL"]
        M1["core (DSL), metadata, effective"]
    end
    subgraph L4["4. FEATURES"]
        F1["Процессоры: resolver, updates, etc."]
    end
    subgraph SPI["SPI Implementations"]
        S1["Генераторы, Резолверы"]
    end
    L1 --> L2
    L2 --> L3
    L3 --> L4
    L2 -.->|"координирует через SPI"| SPI
    L3 -.->|"использует через SPI"| SPI
----

NOTE: Слои определяют направление основных зависимостей (от периферии к ядру).
SPI-реализации (генераторы, резолверы) не являются отдельным слоем — это точки расширения, которые API и MODEL координируют через абстрактные интерфейсы.
Зависимость идёт от реализаций к интерфейсам, а не наоборот.

=== Описание слоёв

[cols="1,2,2",options="header"]
|===
| Слой | Описание | Модули

| INTERFACES
| Два равноправных входа: CLI выполняет операции с metadata.json, Gradle Plugin парсит Kotlin DSL.
| `dependanger-gradle-plugin`, `dependanger-cli`

| API
| Фасад и координация. Единая точка входа для всего функционала. Координирует SPI-реализации через абстрактные интерфейсы.
| `dependanger-api`

| MODEL
| Доменные модели: Core (DSL, сущности), metadata.json, EffectiveMetadata. Определяет SPI интерфейсы.
| `dependanger-core`, `dependanger-metadata-json`, `dependanger-effective`

| FEATURES
| Опциональные процессоры pipeline. Могут быть отключены. Реализуют `EffectiveMetadataProcessor` SPI.
| `dependanger-maven-resolver`, `dependanger-updates`, `dependanger-analysis`
|===

=== SPI-реализации (не слой)

Генераторы артефактов — это SPI-реализации интерфейса `ArtifactGenerator<T>`, а не отдельный архитектурный слой:

[cols="1,2,2",options="header"]
|===
| Тип | Описание | Модули

| Generators
| Преобразуют `EffectiveMetadata` в TOML/BOM. Реализуют `ArtifactGenerator` SPI.
| `dependanger-generator-toml`, `dependanger-generator-bom`
|===

Такой подход соответствует принципу E1 (расширяемость через SPI): новые генераторы добавляются без изменения API или MODEL слоёв.

== C4 Диаграммы

=== Level 1: System Context

См. xref:../00-overview.adoc#system-context[Системный контекст в Overview].

=== Level 2: Container

[mermaid,target=c4-container,format=svg]
----
flowchart TB
    subgraph interfaces ["Interfaces Layer"]
        plugin["Gradle Plugin"]
        cli["CLI"]
    end

    subgraph api ["API Layer"]
        facade["Dependanger<br/>DependangerBuilder"]
    end

    subgraph model ["Model Layer"]
        core["dependanger-core<br/><i>DSL, Models, SPI Interfaces</i>"]
        meta["dependanger-metadata-json<br/><i>Serialization</i>"]
        eff["dependanger-effective<br/><i>Pipeline</i>"]
    end

    subgraph features ["Features Layer"]
        resolver["maven-resolver<br/><i>BOM Import</i>"]
        updates["updates<br/><i>Update Check</i>"]
        analysis["analysis<br/><i>Compatibility</i>"]
    end

    subgraph spi ["SPI Implementations"]
        toml["generator-toml"]
        bom["generator-bom"]
    end

    subgraph external ["External"]
        maven[(Maven Repos)]
    end

    plugin --> facade
    cli --> facade

    facade --> core
    facade --> eff
    facade -.->|"ArtifactGenerator SPI"| toml
    facade -.->|"ArtifactGenerator SPI"| bom

    eff --> core
    eff --> meta
    eff -.->|"EffectiveMetadataProcessor SPI"| resolver
    eff -.->|"EffectiveMetadataProcessor SPI"| updates
    eff -.->|"EffectiveMetadataProcessor SPI"| analysis

    toml --> core
    bom --> core
    resolver --> maven
    updates --> maven
----

NOTE: Пунктирные стрелки обозначают зависимость через SPI интерфейсы.
API и MODEL не зависят от конкретных реализаций — они работают с абстракциями (`ArtifactGenerator`, `EffectiveMetadataProcessor`).
Реализации подключаются через ServiceLoader.

=== Level 3: Component (API Module)

[mermaid,target=c4-component,format=svg]
----
flowchart TB
    subgraph api ["dependanger-api"]
        builder["DependangerBuilder<br/><i>Fluent configuration</i>"]
        facade["Dependanger<br/><i>Main facade</i>"]
        result["DependangerResult<br/><i>Build result</i>"]
    end

    subgraph core ["dependanger-core"]
        dsl["DependangerDsl"]
        models["Domain Models"]
    end

    subgraph effective ["dependanger-effective"]
        pipeline["EffectiveMetadataBuilder"]
        processors["Processors"]
    end

    builder -->|"fromDsl()"| dsl
    builder -->|"build()"| facade
    facade -->|"uses"| pipeline
    facade -->|"returns"| result
    pipeline -->|"runs"| processors
    result -->|"toToml()"| toml["TOML"]
    result -->|"toBom()"| bom["BOM"]
----

== Структура модулей

=== Обзор

[mermaid,target=module-deps,format=svg]
----
flowchart TB
    subgraph interfaces [Interfaces Layer]
        gradle-plugin[gradle-plugin]
        cli[cli]
    end

    subgraph apiLayer [API Layer]
        api[api]
    end

    subgraph modelLayer [Model Layer]
        effective[effective]
        metadata-json[metadata-json]
        core[core]
    end

    subgraph spiImpl [SPI Implementations]
        generator-toml[generator-toml]
        generator-bom[generator-bom]
    end

    subgraph features [Features Layer / SPI Processors]
        maven-resolver[maven-resolver]
        updates[updates]
        analysis[analysis]
        report[report]
    end

    gradle-plugin --> api
    cli --> api

    api --> effective
    api -.->|"SPI"| generator-toml
    api -.->|"SPI"| generator-bom

    effective --> core
    effective --> metadata-json
    effective -.->|"SPI"| maven-resolver
    effective -.->|"SPI"| updates
    effective -.->|"SPI"| analysis

    metadata-json --> core

    generator-toml --> core
    generator-bom --> core
    maven-resolver --> core
    updates --> core
    analysis --> core
    report --> core
    report --> effective

    style interfaces fill:#ADD8E6
    style apiLayer fill:#F08080
    style modelLayer fill:#98FB98
    style spiImpl fill:#FFFFE0
    style features fill:#DDA0DD
----

NOTE: Пунктирные стрелки (`-.->`) обозначают зависимость через SPI интерфейсы.
API и effective модули не имеют compile-time зависимости от конкретных реализаций — они загружаются через ServiceLoader или явную конфигурацию.

=== Описание модулей

[cols="1,2,2,1",options="header"]
|===
| Модуль | Описание | Зависит от | Слой

| `dependanger-core`
| Доменные модели, DSL (`DependangerDsl`), валидация, фильтрация
| —
| MODEL

| `dependanger-metadata-json`
| JSON сериализация через `SerializationFormat` SPI
| core
| MODEL

| `dependanger-effective`
| Processing Pipeline (`EffectiveMetadataBuilder`), резолвинг версий
| core, metadata-json
| MODEL

| `dependanger-api`
| Фасад `Dependanger`, `DependangerBuilder`, `DependangerResult`
| core, effective, generators, features
| API

| `dependanger-generator-toml`
| Генерация Gradle Version Catalog (TOML). Реализует `ArtifactGenerator` SPI.
| core
| SPI

| `dependanger-generator-bom`
| Генерация Maven BOM (XML). Реализует `ArtifactGenerator` SPI.
| core
| SPI

| `dependanger-maven-resolver`
| BOM import процессор (order: 15)
| core
| FEATURES

| `dependanger-updates`
| Update check процессор (order: 70)
| core
| FEATURES

| `dependanger-analysis`
| Compatibility analysis процессор (order: 75)
| core
| FEATURES

| `dependanger-gradle-plugin`
| Gradle Plugin с DSL extension и tasks
| api
| INTERFACES

| `dependanger-cli`
| CLI на Clikt (generate, validate, check-updates, analyze)
| api
| INTERFACES
|===

NOTE: Все модули находятся в `components/` директории.

=== Пакетная структура

[source]
----
io.github.zenhelix.dependanger/
├── core/
│   ├── model/          # Library, Bundle, Version, Plugin, BomImport, TargetPlatform...
│   ├── dsl/            # DependangerDsl, VersionsDsl, LibrariesDsl, SettingsDsl...
│   ├── metadata/       # DependangerMetadata (serializable)
│   ├── validation/     # Validators
│   └── filter/         # LibraryFilter, TagFilter, CompositeFilter
├── effective/
│   ├── model/          # EffectiveMetadata, ProcessingContext
│   ├── builder/        # EffectiveMetadataBuilder
│   ├── pipeline/       # ProcessingPipeline
│   └── processor/      # Built-in processors
├── api/
│   ├── Dependanger.kt
│   ├── DependangerBuilder.kt
│   └── DependangerResult.kt
├── generator/
│   ├── toml/           # TomlGenerator
│   └── bom/            # BomGenerator
├── features/
│   ├── resolver/       # BomImportProcessor
│   ├── updates/        # UpdateCheckProcessor
│   └── analysis/       # CompatibilityCheckProcessor
├── gradle/plugin/      # GradlePlugin, Extension, Tasks
└── cli/                # Clikt commands
----

== Поток данных

=== Обзор

[mermaid,target=architecture-dataflow,format=svg]
----
flowchart TB
    A["Gradle Plugin / CLI"] --> B["DependangerDsl<br/>(in-memory)"]
    B -->|"toMetadata()"| C["metadata.json"]
    C -->|"Processing Pipeline"| D["EffectiveMetadata"]
    D -->|"ArtifactGenerator"| E["libs.versions.toml<br/>bom.pom.xml"]
    E -->|"Publish"| F["Gradle/Maven Projects<br/>(consumers)"]
----

=== Файлы артефактов

[cols="1,2,2",options="header"]
|===
| Файл | Описание | Создаётся

| `metadata.json`
| Сериализованная конфигурация из DSL
| `dependangerGenerateMetadata` task

| `effective-\{distribution\}.json`
| Обработанная модель для distribution
| `dependangerGenerateEffective` task

| `libs.versions.toml`
| Gradle Version Catalog
| `dependangerGenerateToml` task

| `dependanger-bom.pom`
| Maven BOM
| `dependangerGenerateBom` task
|===

=== Version Source Priority

При резолвинге версий:

[source]
----
DECLARED > BOM_IMPORT
----

См. xref:../02-features/F003-processing-pipeline.adoc[F003: Processing Pipeline] для деталей.

== SPI Интерфейсы

Система использует Service Provider Interface (SPI) для расширяемости.

=== SerializationFormat

Абстракция формата сериализации `DependangerMetadata`.

[source,kotlin]
----
interface SerializationFormat<T> {
    val formatId: String  // "json", "yaml"
    val fileExtension: String  // ".json", ".yaml"

    fun serialize(metadata: DependangerMetadata): T
    fun deserialize(input: T): DependangerMetadata
    fun write(metadata: DependangerMetadata, path: Path)
    fun read(path: Path): DependangerMetadata
}
----

**Встроенные реализации:** `JsonSerializationFormat` (`dependanger-metadata-json`)

'''

=== EffectiveMetadataProcessor

Процессор в pipeline обработки.

[source,kotlin]
----
interface EffectiveMetadataProcessor {
    val order: Int          // Порядок выполнения (меньше = раньше)
    val processorId: String // Уникальный идентификатор
    val phase: ProcessingPhase
    val isOptional: Boolean // Можно ли отключить

    suspend fun process(context: ProcessingContext): ProcessingContext
}
----

**Встроенные процессоры:** см. xref:../02-features/F003-processing-pipeline.adoc[F003: Processing Pipeline]

'''

=== ArtifactGenerator

Генератор выходных артефактов.

[source,kotlin]
----
interface ArtifactGenerator<T> {
    val generatorId: String  // "toml", "bom"

    fun generate(effective: EffectiveMetadata): T
    fun write(artifact: T, output: Path)
}
----

**Встроенные реализации:** `TomlGenerator`, `BomGenerator`

'''

=== LibraryFilter

Стратегия фильтрации библиотек.

[source,kotlin]
----
interface LibraryFilter {
    fun filter(libraries: Collection<Library>): Collection<Library>
}
----

**Встроенные реализации:** `TagFilter`, `CompositeLibraryFilter`

== SPI Discovery

Модули регистрируют реализации через `ServiceLoader`:

[source]
----
META-INF/services/
├── io.github.zenhelix.dependanger.core.spi.SerializationFormat
├── io.github.zenhelix.dependanger.effective.spi.EffectiveMetadataProcessor
└── io.github.zenhelix.dependanger.generator.spi.ArtifactGenerator
----

== Программное использование

=== Базовое использование

[source,kotlin]
----
val result = Dependanger.fromDsl {
    versions {
        version("kotlin", "2.1.20")
    }
    libraries {
        library("kotlin-stdlib", "org.jetbrains.kotlin:kotlin-stdlib",
            version = versionRef("kotlin"))
    }
    bomImports {
        bom("spring-boot-bom", "org.springframework.boot:spring-boot-dependencies:3.4.0")
    }
    settings {
        toml { filename = "libs.versions.toml" }
    }
}.build()

// Генерация артефактов
val toml = result.toToml()
val bom = result.toBom()

// Запись в файлы
result.writeTomlTo(Path.of("gradle/libs.versions.toml"))
result.writeBomTo(Path.of("build/dependanger-bom.pom"))
----

=== Builder Pattern

[source,kotlin]
----
val result = Dependanger.fromDsl {
    // DSL конфигурация...
}
.withBomImport()                         // Включить BOM import
.withUpdateCheck()                       // Включить проверку обновлений
.withCompatibilityAnalysis(targetJdk = 17) // Включить анализ совместимости
.forDistribution("android")              // Для конкретного distribution
.build()

// Проверка результата
if (result.diagnostics.hasErrors()) {
    result.diagnostics.errors.forEach { println(it.message) }
}
----

=== Загрузка из файла

[source,kotlin]
----
val result = Dependanger.fromFile(Path.of("metadata.json"))
    .forDistribution("backend")
    .build()
----

== Связанные документы

* xref:domain-model.adoc[Доменная модель] — сущности DSL, Metadata, Effective
* xref:constitution.adoc[Constitution] — архитектурные ограничения (MUST)
* xref:guidelines.adoc[Guidelines] — рекомендации (SHOULD)
* xref:../02-features/F003-processing-pipeline.adoc[F003: Processing Pipeline] — детали pipeline
* xref:../03-interfaces/index.adoc[Интерфейсы] — Gradle Plugin, CLI, API
