= Архитектурные рекомендации (Guidelines)
:toc: left
:toclevels: 3
:sectnums:
:icons: font
:source-highlighter: highlight.js

// ============================================================================
// GUIDELINES — рекомендации, которые СЛЕДУЕТ соблюдать
// В отличие от Constitution, отступление возможно с обоснованием в PR
// ============================================================================

[abstract]
--
Рекомендации по разработке проекта Dependanger.
Следование этим практикам повышает качество кода, но отступление возможно при наличии обоснования.
--

== Соглашения по коду

=== Именование

==== Классы и интерфейсы

[cols="1,2,2",options="header"]
|===
| Тип | Соглашение | Пример

| Interface
| Существительное или прилагательное
| `LibraryFilter`, `Validatable`

| Implementation
| Описательное имя без суффикса `Impl`
| `TagBasedLibraryFilter` (не `LibraryFilterImpl`)

| Abstract class
| Префикс `Abstract` или `Base`
| `AbstractProcessor`, `BaseGenerator`

| Builder
| Суффикс `Builder`
| `LibraryBuilder`, `CatalogBuilder`

| Factory
| Суффикс `Factory`
| `ProcessorFactory`

| Exception
| Суффикс `Exception`
| `ValidationException`, `ResolutionException`
|===

==== Функции

[cols="1,2,2",options="header"]
|===
| Тип | Соглашение | Пример

| Query (возвращает данные)
| `get*`, `find*`, `is*`, `has*`
| `getLibrary()`, `findByTag()`, `isValid()`

| Command (изменяет состояние)
| Глагол в повелительном наклонении
| `validate()`, `resolve()`, `generate()`

| Factory method
| `create*`, `of`, `from`
| `createDefault()`, `of(...)`, `fromJson()`

| Builder method
| `with*`, `without*`
| `withVersion()`, `withoutCache()`
|===

==== Пакеты

```
io.github.zenhelix.dependanger.{module}/
├── api/           # Публичные интерфейсы и DTO
├── impl/          # Реализации (internal)
├── internal/      # Вспомогательные классы
├── spi/           # Service Provider Interfaces
├── dsl/           # DSL builders и extensions
└── model/         # Доменные модели (если специфичны для модуля)
```

=== Kotlin идиомы

==== Data классы

[source,kotlin]
----
// ✅ Используйте copy() для "изменений"
val updated = library.copy(version = newVersion)

// ✅ Используйте destructuring
val (alias, coordinates) = library

// ✅ Named arguments для ясности
Library(
    alias = "kotlin-stdlib",
    coordinates = MavenCoordinates("org.jetbrains.kotlin", "kotlin-stdlib"),
    version = Version("2.1.0")
)
----

==== Sealed классы для ADT

[source,kotlin]
----
// ✅ Sealed для ограниченной иерархии
sealed class ValidationResult {
    data class Success(val metadata: EffectiveMetadata) : ValidationResult()
    data class Failure(val errors: List<ValidationError>) : ValidationResult()
}

// ✅ Exhaustive when
fun handle(result: ValidationResult) = when (result) {
    is ValidationResult.Success -> process(result.metadata)
    is ValidationResult.Failure -> reportErrors(result.errors)
}
----

==== Extension functions

[source,kotlin]
----
// ✅ Для расширения API без наследования
fun Library.hasTag(tag: String): Boolean = tag in tags

// ✅ Для DSL
fun CatalogBuilder.kotlinStdlib(version: String) {
    library("kotlin-stdlib") {
        group = "org.jetbrains.kotlin"
        artifact = "kotlin-stdlib"
        this.version = version
    }
}

// ⚠️ Избегайте extension functions, меняющих состояние
// (предпочитайте методы класса)
----

==== Coroutines

[source,kotlin]
----
// ✅ Suspend функции для async операций
suspend fun checkUpdates(): UpdateReport

// ✅ Явный диспетчер для блокирующего IO
suspend fun fetchMetadata() = withContext(Dispatchers.IO) {
    httpClient.get(url)
}

// ✅ Flow для потоков данных
fun watchForChanges(): Flow<ChangeEvent>

// ⚠️ Избегайте GlobalScope
// ❌ GlobalScope.launch { ... }
----

=== Обработка ошибок

==== Иерархия исключений

[source,kotlin]
----
// Базовое исключение проекта
sealed class DependangerException(
    message: String,
    cause: Throwable? = null
) : RuntimeException(message, cause)

// Категории ошибок
class ValidationException(message: String) : DependangerException(message)
class ResolutionException(message: String, cause: Throwable?) : DependangerException(message, cause)
class GenerationException(message: String) : DependangerException(message)
----

==== Паттерны обработки

[source,kotlin]
----
// ✅ Result для ожидаемых ошибок
fun resolveVersion(library: Library): Result<Version>

// ✅ Sealed class для rich error info
sealed class ResolutionResult {
    data class Resolved(val version: Version) : ResolutionResult()
    data class NotFound(val coordinates: MavenCoordinates) : ResolutionResult()
    data class NetworkError(val cause: IOException) : ResolutionResult()
}

// ✅ require/check для preconditions
fun process(libraries: List<Library>) {
    require(libraries.isNotEmpty()) { "Libraries list cannot be empty" }
    check(isInitialized) { "Processor must be initialized" }
}
----

== Паттерны проектирования

=== Builder Pattern

[source,kotlin]
----
// ✅ Fluent builder с DSL
class CatalogBuilder {
    private var name: String = ""
    private val libraries = mutableListOf<Library>()
    
    fun name(value: String) = apply { name = value }
    
    fun library(alias: String, block: LibraryBuilder.() -> Unit) = apply {
        libraries += LibraryBuilder(alias).apply(block).build()
    }
    
    fun build(): Catalog {
        require(name.isNotBlank()) { "Name is required" }
        return Catalog(name, libraries.toList())
    }
}

// Использование
val catalog = CatalogBuilder()
    .name("my-catalog")
    .library("kotlin-stdlib") {
        group = "org.jetbrains.kotlin"
        artifact = "kotlin-stdlib"
        version = "2.1.0"
    }
    .build()
----

=== Strategy Pattern (для алгоритмов)

[source,kotlin]
----
// ✅ Интерфейс стратегии
interface VersionResolutionStrategy {
    suspend fun resolve(library: Library): Version?
}

// Реализации
class LatestStableStrategy : VersionResolutionStrategy { ... }
class LatestAnyStrategy : VersionResolutionStrategy { ... }
class FixedVersionStrategy(private val version: Version) : VersionResolutionStrategy { ... }
----

=== Chain of Responsibility (для pipeline)

[source,kotlin]
----
// ✅ Процессор в цепочке
interface EffectiveMetadataProcessor {
    val order: Int
    suspend fun process(metadata: EffectiveMetadata): EffectiveMetadata
}

// Pipeline исполнитель
class ProcessingPipeline(
    private val processors: List<EffectiveMetadataProcessor>
) {
    suspend fun execute(initial: EffectiveMetadata): EffectiveMetadata {
        return processors
            .sortedBy { it.order }
            .fold(initial) { metadata, processor ->
                processor.process(metadata)
            }
    }
}
----

== Рекомендации по модулям

=== Core модуль

**Должен содержать:**

* Доменные модели (`Library`, `Catalog`, `Version`, etc.)
* Интерфейсы для SPI
* Базовая валидация
* DSL markers и базовые builders

**Не должен содержать:**

* Реализации, зависящие от внешних систем
* HTTP клиенты
* Конкретные форматы вывода (TOML, BOM)

=== API модуль

**Должен содержать:**

* Facade (`Dependanger`, `DependangerBuilder`)
* Координация между компонентами
* Конфигурация по умолчанию
* ServiceLoader discovery

**Паттерн фасада:**

[source,kotlin]
----
class Dependanger private constructor(
    private val config: DependangerConfig
) {
    suspend fun generate(): GenerationResult { ... }
    suspend fun validate(): ValidationResult { ... }
    suspend fun checkUpdates(): UpdateReport { ... }
    
    companion object {
        fun builder(): DependangerBuilder = DependangerBuilder()
    }
}
----

=== Feature модули

**Каждый feature модуль должен:**

* Реализовывать один SPI интерфейс
* Быть независимым от других features
* Регистрироваться через ServiceLoader или явную конфигурацию

=== Interface модули (CLI, Gradle Plugin)

**Должны:**

* Быть тонким слоем над API
* Транслировать пользовательский ввод в API вызовы
* Форматировать вывод для конкретной платформы

**Не должны:**

* Содержать бизнес-логику
* Напрямую обращаться к core, минуя api

== Тестирование

=== Структура тестов

```
src/test/kotlin/
├── unit/                    # Быстрые изолированные тесты
│   └── {module}/
│       └── {Class}Test.kt
├── integration/             # Тесты с реальными зависимостями
│   └── {feature}IT.kt
└── functional/              # E2E тесты
    └── {scenario}FT.kt
```

=== Naming convention для тестов

[source,kotlin]
----
class LibraryFilterTest {
    @Test
    fun `should include library when tag matches`() { ... }
    
    @Test
    fun `should exclude library when no tags match`() { ... }
    
    @Test
    fun `should throw when filter is empty`() { ... }
}
----

=== Test fixtures

[source,kotlin]
----
// В отдельном модуле test-fixtures
object TestLibraries {
    val kotlinStdlib = Library(
        alias = "kotlin-stdlib",
        coordinates = MavenCoordinates("org.jetbrains.kotlin", "kotlin-stdlib"),
        version = Version("2.1.0")
    )
}
----

== Связанные документы

* xref:constitution.adoc[Constitution] — обязательные правила
* xref:fitness-functions.adoc[Fitness Functions] — автоматические проверки
* xref:architecture.adoc[Architecture] — общая структура
